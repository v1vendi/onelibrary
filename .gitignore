# Real captures contain the user's personal music metadata. Never commit.
python/tests/corpus/
*.db
*.db-shm
*.db-wal
# ...except the viewer's fixture, which is synthetic (Alpha/Beta/Gamma, device
# TESTDEV) and is what `npm test` decrypts. Without it a fresh clone cannot
# run the viewer suite, and neither can CI.
!viewer/test/fixtures/sample.db
__pycache__/
*.pyc
.venv/
dist/
build/
*.egg-info/
.pytest_cache/
.ruff_cache/
viewer/dist/
viewer/node_modules/

# macOS
.DS_Store
